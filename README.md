App.tsx
 ├─ Header            (usa useAuth + useCart directo — bonus, nivel 1)
 └─ ProductsPage       (nivel 1)
     └─ ProductGrid    (nivel 2, usa useProducts)
         └─ ProductCard (nivel 3 — usa useCart directo, SIN recibir cart por props)